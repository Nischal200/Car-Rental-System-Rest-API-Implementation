
#Configure my sql details
#url=currently localhost might replace with ip address,username,password
spring.datasource.url = jdbc:mysql:link:port/crs?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root
spring.datasource.password=Interview002121

#Setting own properties profile


#Hibernate properties being configured

#Add dialect properties for the database being used in this case MySql5***.

#spring.jpa.properties.hibernate.dilect = org.hibernate.dialect.MySql5InnoDBDialect 

#Generate database automatically using hibernate.

#Creates table if no table else updates table

spring.jpa.hibernate.ddl-auto=update

#Allows us to see hibernate generated query

logging.level.org.hibernate.sql =debug
